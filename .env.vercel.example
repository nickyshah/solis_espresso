# Vercel Postgres: set DATABASE_URL to POSTGRES_PRISMA_URL
DATABASE_URL=${POSTGRES_PRISMA_URL}

NEXTAUTH_URL=https://your-vercel-domain.vercel.app
NEXTAUTH_SECRET=replace-with-a-long-random-string
ADMIN_EMAIL=admin@solisespresso.com
ADMIN_PASSWORD=admin123

# Vercel Blob requires no extra env for public uploads in this setup
# (but you can scope tokens if desired)
